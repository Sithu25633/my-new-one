
// This is the hard-coded secret code to pass the security gate.
// In a real application, this should be an environment variable.
export const SECRET_SECURITY_CODE = '2005';

// The base URL for the backend API.
// Replace 'http://localhost:4000' with your actual VPS IP address and port in production.
export const API_BASE_URL = 'http://45.151.155.127';
